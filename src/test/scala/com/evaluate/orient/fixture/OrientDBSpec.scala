package com.evaluate.orient.fixture

import org.scalatest.{FlatSpec, ShouldMatchers}

class OrientDBSpecTest extends FlatSpec with ShouldMatchers  {
   "A graph database" should "be capable of storing 300M records" in {
   }
 }
